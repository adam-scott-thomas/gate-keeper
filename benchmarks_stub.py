"""Benchmark integration stub for gate-core — STUB.

Seeded by Creator 4. Improvers: wire into CI or pre-commit.

Pattern:
    from gate_bench.scenarios import bench_filter_normal
    result = bench_filter_normal(n_tools=10, iterations=1000)
    assert result.p99_us < 50, f"Regression: p99={result.p99_us}us (limit: 50us)"
"""
